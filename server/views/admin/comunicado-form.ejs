<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= comunicado ? 'Editar Comunicado' : 'Nuevo Comunicado' %> - Admin FCE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; }
    .form-container { max-width: 800px; margin: 2rem auto; }
    .btn-back { margin-bottom: 1rem; }
  </style>
</head>
<body>
  <div class="container form-container">
    <a href="/admin/comunicados" class="btn btn-secondary btn-back">← Volver a Comunicados</a>
    
    <h2><%= comunicado ? 'Editar Comunicado' : 'Nuevo Comunicado' %></h2>
    
    <form method="POST" action="<%= comunicado ? `/admin/comunicados/${comunicado.id}` : '/admin/comunicados' %>">
      <div class="mb-3">
        <label for="titulo" class="form-label">Título *</label>
        <input type="text" class="form-control" id="titulo" name="titulo" 
               value="<%= comunicado?.titulo || '' %>" required>
      </div>
      
      <div class="mb-3">
        <label for="contenido" class="form-label">Contenido</label>
        <textarea class="form-control" id="contenido" name="contenido" rows="6">
<%= comunicado?.contenido || '' %></textarea>
      </div>
      
      <div class="mb-3">
        <label for="fecha" class="form-label">Fecha *</label>
        <input type="date" class="form-control" id="fecha" name="fecha" 
               value="<%= comunicado?.fecha || '' %>" required>
      </div>
      
      <div class="mb-3">
        <label for="archivo" class="form-label">Archivo (URL del PDF o documento)</label>
        <input type="text" class="form-control" id="archivo" name="archivo" 
               placeholder="Ej: /docs/comunicado1.pdf" 
               value="<%= comunicado?.archivo || '' %>">
        <div class="form-text">Deja vacío si no hay archivo adjunto</div>
      </div>
      
      <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <button type="submit" class="btn btn-primary">
          <%= comunicado ? 'Actualizar' : 'Crear' %>
        </button>
      </div>
    </form>
  </div>
</body>
</html>