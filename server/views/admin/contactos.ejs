<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Contactos - Admin FCE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <h1>Mensajes de Contacto</h1>
    <a href="/admin" class="btn btn-secondary mb-3">← Volver al Panel</a>
    
    <div class="row">
      <% contactos.forEach(c => { %>
        <div class="col-md-6 mb-3">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= c.nombre %> (<%= c.oficina %>)</h5>
              <h6 class="card-subtitle mb-2 text-muted"><%= c.email %> | <%= c.telefono || 'Sin teléfono' %></h6>
              <p class="card-text"><%= c.mensaje %></p>
              <small class="text-muted">Enviado: <%= new Date(c.createdAt).toLocaleString('es-PE') %></small>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Exportar Contactos</h1>
        <a href="/admin/contactos/exportar-pdf" class="btn btn-success">📄 Exportar a PDF</a>
    </div>
  </div>
</body>
</html>