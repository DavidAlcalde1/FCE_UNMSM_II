<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= maestria ? 'Editar Maestría' : 'Nueva Maestría' %> - Admin FCE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; }
    .form-container { max-width: 800px; margin: 2rem auto; }
    .btn-back { margin-bottom: 1rem; }
  </style>
</head>
<body>
  <div class="container form-container">
    <a href="/admin/maestrias" class="btn btn-secondary btn-back">← Volver a Maestrías</a>
    
    <h2><%= maestria ? 'Editar Maestría' : 'Nueva Maestría' %></h2>
    
    <form method="POST" action="<%= maestria ? `/admin/maestrias/${maestria.id}` : '/admin/maestrias' %>" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="nombre" class="form-label">Nombre de la Maestría *</label>
        <input type="text" class="form-control" id="nombre" name="nombre" 
               value="<%= maestria?.nombre || '' %>" required>
      </div>
      
      <div class="mb-3">
        <label for="imagen" class="form-label">Imagen</label>
        <% if (maestria && maestria.imagen) { %>
          <div class="mb-2">
            <img src="/<%= maestria.imagen %>" alt="Vista previa" style="max-height: 150px; max-width: 150px; object-fit: cover; border-radius: 4px;">
          </div>
        <% } %>
        <input type="file" class="form-control" id="imagen" name="imagen" accept="image/*">
        <% if (maestria && maestria.imagen) { %>
          <div class="form-text">Deja vacío para mantener la imagen actual</div>
        <% } %>
      </div>
      
      <div class="mb-3">
        <label for="enlace" class="form-label">Enlace (URL)</label>
        <input type="text" class="form-control" id="enlace" name="enlace" 
               placeholder="https://ejemplo.com/maestria"
               value="<%= maestria?.enlace || '' %>">
      </div>
      
      <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <button type="submit" class="btn btn-primary">
          <%= maestria ? 'Actualizar' : 'Crear' %>
        </button>
      </div>
    </form>
  </div>
</body>
</html>